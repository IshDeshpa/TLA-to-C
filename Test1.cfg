# Configuration file for TLA+ specification

# The specification (e.g., the module name or description of the specification)
SPECIFICATION
Test

# Define constants
CONSTANTS
C1 = 10
C2 = 20

# Define invariants (if any)
INVARIANTS
x \in Nat

# Define properties (e.g., properties that you want to check or verify)
PROPERTIES
x > 0
